#Automatic Tagger

This script uses a bag-o-words approach and Logistic Regression to suggest tags for a set of untagged input data (raw_data.csv) given pre-tagged input data (training_data.csv).  Customize auto-tag.py to your data.